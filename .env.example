# Main PostgreSQL database (profiles, devices, locations)
DATABASE_URL=postgresql://user:pass@host:port/database

# Events TimescaleDB (event_log)
EVENTS_DATABASE_URL=postgresql://user:pass@host:port/database
